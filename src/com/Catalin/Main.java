package com.Catalin;

public class Main {

    public static void main(String[] args) {
        /** The keyword super is used to access/call the parent class members (variables and methods).
         * The keyword this is used to call the current class members(variables and methods). This is
         * required when we have a parameter with the same name as an instance variable (field).
         *
         * Note: we can use both of them anywhere in a class except static areas (the static block or a
         * static method). Any attempt to do so will lead to compile-time errors.
         */
    }
}
